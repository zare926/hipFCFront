import useSound from 'use-sound'
import { AppThunk } from '../app/store'
import DayByDay from '../assets/music/daybyday.mp3'

export const musicControl = (): AppThunk => {
  return async (dispatch): Promise<void> => {}
}
